/* Site-wide strings. `title` is the homepage/browser title; `name` is the
   suffix on every other page. Per-page front matter `title` drives the h1. */
export default {
  name: "Zigarot",
  title: "Zigarot - Australia Variety Creator"
};
